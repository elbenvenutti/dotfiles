" open NERDTree on vim startup if no files specified

" autocmd StdinReadPre * let s:std_in=1
" autocmd VimEnter * if argc() == 0 && !exists("s:std_in") | NERDTree | endif

" show hidden files by default

let NERDTreeShowHidden=1
